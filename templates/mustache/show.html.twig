{% extends 'base.html.twig' %}

{% block title %}Mustache{% endblock %}

{% block body %}
    <h1>Mustache</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mustache.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mustache.name }}</td>
            </tr>
            <tr>
                <th>Function</th>
                <td>{{ mustache.function }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('home',{'id': mustache.id} ) }}">editor</a>

    <a href="{{ path('mustache_edit', {'id': mustache.id}) }}">edit</a>

    {{ include('mustache/_delete_form.html.twig') }}
{% endblock %}
